name: video_compress_v2
description: "A new Flutter video compress, support HDR convert to SDR"
version: 1.0.0+1
homepage:

environment:
  sdk: '>=3.2.3 <4.0.0'
  flutter: '>=3.3.0'

dependencies:
  flutter:
    sdk: flutter
  plugin_platform_interface: 2.1.8
  # 内嵌的 lat_hdr_transcoder_v2 (含 SPM 适配)，原 pub.dev 1.0.0+5 上游停滞且无 SPM 支持
  lat_hdr_transcoder_v2:
    path: ./plugin/lat_hdr_transcoder_v2


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  plugin:
    platforms:
      android:
        package: com.video_compress_v2.video_compress_v2
        pluginClass: VideoCompressV2Plugin
      ios:
        pluginClass: VideoCompressV2Plugin

